#include <stdio.h>
#include <stdlib.h>

int loop() {
    int result;
    
    if (i != limit && i != limit - 1)
        return ();
    return(values[i]);
}

int main(void) {
    int ret;
    int values[10000], limit;

    scanf("%d", &limit);
    int i = 0;
    printf("%d", ret);
    return 0;
}
